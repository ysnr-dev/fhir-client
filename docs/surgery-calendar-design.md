# 手術室カレンダーとダブルブッキング防止の設計

手術オーダー第1段階(`docs/surgery-order-design.md` §7-3)の残りだった、手術室の
可視化・部屋の取り合いの防止・曜日ごとの科割り当てをまとめて扱う。

実装日: 2026-08-28。

本文中の区別:
- **［事実］** = FHIR R4 / JP Core の仕様、または上流・既存実装で確認した内容
- **［導出］** = 仕様から論理的に導ける内容
- **［提案］** = 本ドキュメントの設計判断

---

## 1. 予約枠(Slot)を使わない判断は変えない

第1段階で「手術は予約枠を持たない」と決めた(`surgery-order-design.md` §1)。
カレンダーを入れるにあたって作り直す選択肢もあったが、そのままにした。理由は 4 つ。

1. **［事実］枠は事前量産で、手術の時間は可変**。`SlotPattern`(曜日 × 時間帯 ×
   1 枠の長さ × 定員)から固定長の枠を作り置きする方式なのに対し、手術の所要時間は
   術式ごとに 30 分〜8 時間まで散らばり、開始時刻も枠境界に乗らない(前の手術が
   延びればその日の全部が後ろへずれる)。5 分刻みで作れば 1 部屋 1 日で 200 件近い
   Slot になり、しかも毎日ずらす。
2. **［事実］申し込む人と日程を決める人が違う**。既存 4 種は
   `buildExamAppointmentEntries` のとおりオーダーの transaction に予約を同梱する
   先着モデル。手術は診療科が日程未定で申し込み、手術部が後から組む(§5.2)。
   枠を取る操作が申込から切り離されている。
3. **［導出］多資源の同時確保が表現できない**。`AppointmentSlotPicker` は枠表を
   1 つ選ぶ作りで、複数枠表の空きの AND を取れない。手術は 手術室 × 執刀医 ×
   麻酔科医 の同時空きが要る。(部屋 × 執刀医)の組み合わせごとに枠表を作れば
   組み合わせ爆発するし、麻酔科医は当日まで決まらないことも多い。
4. **［導出］ブロックスケジュールの融通が枠表分割では効かない**。`Schedule.specialty`
   で部屋 × 曜日 × 科の枠表に割っても、「割当科が使わない枠を前日に他科へ開放する」を
   別枠表の Slot 間で動かせない。

［提案］したがって**部屋の空き状況の真実の源は手術オーダーそのもの**
(`occurrenceDateTime` + 所要時間のローカル拡張)のままにする。カレンダーはその
読み替えで、新しいリソースは作らない。**上流 fhir-server の改修は不要**
(使う検索は既存の `occurrence` だけ)。

［導出］Slot を捨てた代償は排他の仕組みが無いこと。そこを埋めるのが §3。

---

## 2. 手術室カレンダー

独立した画面(`/surgery-calendar`、部門業務メニュー)。日付と表示単位(日/週)は
`SurgeryCalendarPage` が持つ。

［改訂］当初は手術一覧(`/surgery-worklist`)の 3 つ目のタブで、日付を一覧と共有していた。
**画面を分けた** —— 一覧は「その日の手術を 1 件ずつ処理する」画面で進捗を進めながら縦に
読み、カレンダーは「空いているところを探して入れる」画面で部屋 × 時刻の面を横に読む。
使う場面も見る向きも違い、一覧の絞り込み(手術室・病棟・診療科・ステータス)はカレンダーで
1 つも使っていなかった —— タブに同居させる理由が無い。日付の共有も、一覧で見ていた日を
そのまま持ち越したい場面が実際には無く(組むのは先の日、処理するのは当日)、失っても困らない。

［実装］読むクエリは一覧と同じ `useSurgeryWorklist(date)`。**queryKey が同じ**なので
画面を移っても読み直しは起きない(タブだった頃と変わらない)。週表示は
`useSurgeryWorklistWeek` が同じクエリを 7 本並べるだけで、週から日へ降りるときも
キャッシュがそのまま効く。

#### 列(手術室)の並び

「場所」マスタの**表示順**(`location-display-order` 拡張、`fhir/locationHelpers.ts`)。
日ビューの列も週ビューの行も同じ並びで、`useLocationOptions` が読んだ時点で
`sortLocations` により 表示順 → 名称 に並ぶ。

［提案］並び順は backend のマスタを起こさず **Location そのものの拡張**として持つ。
並びは部屋の属性で、部屋を消せば一緒に消えるのが自然 —— `location_id` で紐づく別
テーブルだと孤児行の掃除が要る。施設で 1 つの並びになるのも都合がよい(端末ごとの
好みではなく、手術室の並びは手術部で共有された前提)。設定の置き場所も「場所」マスタ
という既にあるものが使える。

［事実］上流は Location の独自拡張を素通しし、往復もする(2026-08-28 に確認)。
［実装］拡張では `_sort` できないので並べ替えは読み手側。`useLocationOptions` は
`_count=100` の全件読みなので、取りこぼしは出ない。
［実装］`buildLocation` はリソースを組み立て直すので、表示順はそこで必ず載せ直す
(落とすと場所を編集するたびに消える)。
［導出］未設定の部屋は**末尾**に回す(設定済みと混ぜて 0 扱いにすると、1 つ設定した
だけで他の部屋が押し出される)。マスタに無いがその日使われている部屋も後ろに付く。

### 2.1 日ビュー(横 = 手術室、縦 = 時刻)

当日の運用把握と、部屋の重なりの視認。

- 列は**登録済みの手術室(Location 種別 `SU`) ∪ その日に使われている部屋**。
  マスタから消えた部屋に予定が残っていても列が消えないようにする。
- 1 分 = 1px の絶対配置。時間軸は既定 8:00-18:00 で、はみ出す予定があれば
  その ぶんだけ広げる(夜間の緊急手術が格子の外に出ないように)。
- **［提案］日をまたぐ手術は 24:00 で切る**。またぎを正しく描くには翌日の列が要り、
  1 日の格子という前提が崩れる。長時間手術の終了時刻は一覧・詳細で読む。
- **重なりは列の中で横に分ける**。重なりの塊ごとに必要なレーン数を数え、その塊の
  全員を同じ分割数で描く(幅が揃って読みやすい)。重なったカードは警告色。
- **中止は出さない**(部屋を空けるので、一覧・重なり判定と同じ扱い)。
- **格子に置けない手術は右の未確定リストへ出す**(§2.4)。格子に置けないからと
  落とすと、予定が消えて見える。

#### カードの中身

上から ステータス + 入室〜退室予定 / 患者(番号・氏名(カナ)・年齢・性別) /
主術式 / 執刀医。見出し行(1 行目)と本文の間だけ余白を空けて、2 つのまとまりに見せる。

［提案］時刻は**終了予定まで出す**(`rangeLabel`。一覧・確認モーダルと同じ書式)。
所要時間はカードの高さでも分かるが、重なりを詰めるときに読みたいのは数字の方。

［導出］**カードの高さは所要時間で決まる**ので、短い手術では下の行が隠れる。
そこで**隠れて困る度合いが低いものを下に置く**。当日の手術部が見るのは「今どの部屋で
誰の何が動いているか」なので、ステータスと患者を上に置き、執刀医を最後にした。
全文は title(ツールチップ)に入れてあるので、隠れても読めなくはならない。

［提案］患者氏名を出す。第1段階の `SurgeryRoomDaySchedule`(§5.5)は患者名を
**出さない**判断をしたが、あちらはカルテの申込フォームからも開く「空き確認」の
表示で、無関係な患者の氏名を並べる必要がなかった。カレンダーは手術部の業務画面で、
一覧タブと同じ情報が並ぶ場所なので、そろえて出す。

［提案］ステータスは色を 3 つに分ける(これから = 受付済 / 進行中 = 入室中 /
済 = 実施済)。申込済は色を付けない —— まだ手術部が触っていない状態で、
カレンダー上で目立たせる意味がない。

［提案］**強弱は「ステータス + 時刻」と「患者氏名」に寄せる**。カードを離れて見た
ときに拾いたいのはこの 2 つで、他は近づいて読む情報。具体的には見出し行
(ステータスの枠付きバッジ + 時刻)と氏名を太字・`--text-h`・本文より 1px 大きく、
カナだけ括弧書きの補足として小さく保つ。**患者番号・年齢・性別は本文と同じ大きさ**
にする(氏名を特定するための添え物だが、読めなければ意味がないので縮めない)。
なお年齢・性別に `.order-select__muted` は使わない —— padding が付いて行が間延びする。

#### カードの操作(進捗ボタンとケバブ)

見出し行の右端に、一覧タブ(予定日別)と同じ組み方で置く。押せる操作・遷移先は
`surgeryTaskActions` が持っているものをそのまま使う。

- **普段押す操作をボタンに**(申込済 → 受付 / 受付済 → 入室 / 入室中 → 実施)。
  格子の中の操作なので一覧の行ボタンより一段小さくする。
- **「実施」は実施入力モーダルを開く**。ステータスを進めるだけでなく実施記録を
  書く操作なので、一覧タブと同じ `SurgeryPerformModal` に繋ぐ。
- **押し間違えると進捗が巻き戻る操作(取消・中止)はケバブに畳む**。麻酔チャート
  (入室中・実施済でだけ開ける)と「カルテ表示」「編集」も同じメニューに入れる。
  カルテ・麻酔チャートへは一覧の患者名リンクと同じく `useReturnLinkState` で戻り先を渡す。

- **決めた日程を外して未確定リストへ戻す**「日付未定に変更」「部屋未定に変更」も
  ケバブに置く。格子から待ち行列への逆向きの動きで、ドラッグでは表せない
  (未確定リストは落とし先ではない)。

［導出］この 2 つは**申込済のときだけ**出す。受付済から先は手術部が日程を確定した後で、
外すと病棟・麻酔科が見ている予定が黙って消える。戻すなら受付を取り消してから
(その導線は同じメニューの「受付取消」)。

［導出］外すのは押した片方だけ。もう片方と所要時間は残して、組み直すときの手掛かりに
する(部屋未定にしても日時は残るので、右ペインの「部屋未定」に時間帯付きで並ぶ)。

［実装］書き込みはドラッグでの移動と同じ `useMoveSurgerySchedule`。進捗(Task)は
触らないので申込済のまま動かない。
［実装］日付を外すときは `occurrenceDateTime` を**消す**(空文字だと `occurrence:missing`
で拾えず、未確定リストに出てこない)。ついでに古いデータの `occurrencePeriod` も落とす
—— occurrence[x] は排他で、2 つ並ぶと読み手によってどちらを見るかがぶれる。

［導出］メニューの並びは **カルテ表示 → 麻酔チャート → 日程を外す → 編集 → 取消・中止**。
「別の画面へ行く → 日程を外す → この申込を直す → 進捗を戻す」の順で、いちばん危ない
操作が下に来る。日程を外す 2 つは 1 クリックで効くので、フォームを開くだけの「編集」を
挟んで取消・中止から離す。
**カルテ表示はどの進捗でも先頭**に固定する —— 一番よく押すうえ、進捗で出たり消えたり
する項目(麻酔チャート・取消の種類)の下に置くと、カードごとに位置がずれて探す羽目になる。

［導出］1 つ前に戻す操作は「取消」ではなく **「受付取消」「入室取消」「実施取消」**と
取り消す対象を名前に入れる(`surgeryTaskActions`)。手術は受付から実施まで段が多く、
ただの「取消」ではどこまで戻るのかがメニューの中で分からない。

［導出］操作は**必ず見出し行(1 行目)に置く**。カードの高さは所要時間で決まり、
短い手術では下の行から隠れるため、下に置くと押せないカードが生まれる。

［実装］絶対配置ではなく flex で右へ押し出す(`margin-left: auto`)。絶対配置だと
狭いカードで時刻の文字と重なる。

［実装］**この塊の pointerdown は止める**。止めないとボタンを押した指がそのまま
カードのドラッグを始め、押す前にカードが動く。
カレンダーは `overflow` の中にあるので `RowMenu` は `escapesClipping` で開く。

［改訂］当初は「週ビューのチップには操作を付けない(セルが button なので入れ子に
なる)」としていたが、§2.6 で修正の導線を入れるときにセルを `div[role=button]` へ
変えた。チップごとのケバブは**編集だけ**に絞る —— 進捗の操作は時刻の見える日ビュー
でするもので、週ビューは日を選ぶ画面という位置づけは変えない。

### 2.2 週ビュー(横 = 曜日、縦 = 手術室)

日程を組むときの空き探し。格子は予約枠カレンダー(`.slot-calendar`)を借り、
セルの中身だけを手術用に組んだ。

- セルはその日その部屋の件数 + 各手術のチップ(時刻 + 主術式)。
- 重なりのある日はセルを警告色にする(週で見ても取りこぼさない)。
- セルと日付見出しのクリックでその日の日ビューへ降りる。

### 2.3 ドラッグ＆ドロップで日程を動かす

カード/チップを掴んで動かせる。

- **動かせるのは 申込済・受付済 だけ**(`isSurgeryMovable`)。入室中・実施済はもう
  起きた事実で、予定日時を後から動かすと実施記録と食い違う(訂正は実施入力の担当)。
  中止はそもそもカレンダーに出ない。掴めるカードだけカーソルが変わる。
- **日ビュー**: 縦で入室時刻(**5 分刻み**)、横の列で手術室。日付は変わらない。
- **週ビュー**: 別のセルへ落として日付と手術室。**時刻は動かさない**
  (セルは 1 日ぶんの箱で、その中のどこに落ちたかに意味が無いため)。
- **掴みしろを保つ**。カードの真ん中を持ったのに頭がポインタへ飛ぶと、掴んだ瞬間に
  予定が動いたように見える。掴んだ位置とカードの頭のずれを引いてから丸める。
- 日をまたぐ移動は作らない(0:00〜24:00 にクランプ)。§2.1 と同じ理由。
- ［事実］HTML5 の Drag and Drop は使わない(ドラッグ像を制御できず、タッチが
  ブラウザ任せになる)。Pointer Events で自前で追う(`hooks/useCardDrag.ts`。
  既存の前例は `KarteSplitter`)。**4px 未満はクリック扱い**でドラッグを始めず、
  Escape とポインタのキャンセルで中断できる。
- ［実装］週ビューのセルは押すと日ビューへ降りるので、**ドラッグ直後の click は飲む**
  (`consumeClick`)。掴んで離しただけで画面が変わるのを防ぐ。
- ［実装］pointermove / up は window で拾う。掴んだ要素の外(別の列)へ出ても追う
  必要があり、`setPointerCapture` だと移動先の列でイベントが要素に届かない。

#### カードの縁を掴んで時間を変える(リサイズ)

日ビューのカードは上下の縁を掴んで伸縮できる。**掴んだ側の端だけを動かし、反対の端は
据え置く** —— 上端なら入室時刻(退室はそのまま)、下端なら所要時間が変わる。移動と同じ
条件(申込済・受付済のみ)で、刻みも移動と同じ **5 分**。

［導出］所要時間を直すのに編集フォームを開かせない。手術部が日程を詰めるときに触るのは
「何時から」と「何分」で、その 2 つは格子の上で見比べながら決まる。フォームを開くと
格子が見えなくなり、隣の予定との間合いが分からなくなる。

［導出］**見えるハンドルも説明のツールチップも出さない**。カードの高さは所要時間その
もので、短い手術では帯 1 本ぶんが本文を潰す。縁に `title` を付けるとカード全体の
title(時刻・患者・術式)を縁の上でだけ潰してしまう。掴める幅(6px)だけ確保し、合図は
カーソル(`ns-resize`)とホバー時の縁の色に任せる。

［導出］引いている間、時刻の行を**引いている今の値**に差し替え、所要時間(分)も添える。
高さが変わるだけでは何分になったのか読めない。カードはその場で背が伸びるので、確認
モーダルを開く前に「これでいいか」をカードのまま決められる。

［実装］縁の pointerdown は**カードの上で止める**(`stopPropagation`)。止めないと同じ指が
カードの移動も始める。ポインタは縁自身で捕まえる(`setPointerCapture`)—— カードの外・
列の外まで引いても追える。移動が window で拾うのと違う作りだが、伸縮は掴んだ列から
出ない操作なので、捕まえた方が単純になる。

［実装］掴んで戻しただけ(値が変わらない)なら何もしない。移動と同じ扱い。

［実装］最短は 1 目盛り(`DRAG_SNAP_MINUTES`)。反対側の端を追い越させない。

#### ドロップで即書き込みにはしない

［提案］落とした時点では書かず、**変更の確認**(`SurgeryMoveConfirmModal`)を挟む。
リサイズも同じ確認を通る —— 変更後の時間帯で重なりを引き直すので、**伸ばした先の
重なりも拾える**(`useTargetConflicts` に所要時間を渡すのはこのため)。
見出しと文言は変わる内容に合わせる: 日・部屋が変わらなければ「手術の時間を変更」、
変われば「手術の日程を移動」、日程が初めて入るときは「手術の日程を決める」。
日程は手術部・病棟・麻酔科が見ている予定で、手が滑ったドラッグがそのまま他部署へ
流れると取り返しが面倒になる。確認では変更前 → 後を並べ、**移動先の日を引き直して**
(キャッシュを見ない。§3-1 と同じ理由)重なりを出す。重なっていればボタンが
「重なりを承知で移動」に変わる —— 止めはしないが、承知したことは残る。

割当科の食い違いも同じモーダルに出すが、これは警告まで(§4-2)。

書き込みは `buildSurgeryMoveBundle`(occurrence と所要時間・手術室の拡張だけ差し替え、
他の拡張は保持)。**Task には触らない**。確定モーダルの「未定 → 確定」は受付済へ
進める操作だが、こちらは既に日程の入った手術を動かすだけで、手術部が受け付けた
事実は日程を動かしても変わらない。

### 2.4 未確定リスト(右ペイン)

［提案］**格子に置けない手術を右に集め、そこから格子へドラッグして日程を決める**。
カレンダー導入時は「時刻・手術室が未定」を格子の下に箇条書きで出していたが、
落とし先の格子から遠く、掴んで動かす相手にもなっていなかった。左右に分けて
**待ち行列 → 格子** の向きに揃える(`SurgeryPendingPanel`)。

出す 3 組は「なぜ格子に置けないか」で分かれる。

| 組 | 中身 | 材料 |
|---|---|---|
| 日付未定 | 予定手術日を入れずに申し込まれたもの | `useSurgeryUnscheduledList`(一覧タブと同じクエリ) |
| 部屋未定 | 見ている範囲に日付はあるが手術室が無い | 表示中の日 / 週の一覧 |
| 時間未定 | 手術室はあるが入室時刻が無い | 表示中の日の一覧(**日ビューだけ**) |

- ［導出］**時間未定は週ビューには出さない**。週のセルは 1 日ぶんの箱で時刻を要らず、
  時刻の無い手術も格子の中に置けている(チップに「時刻未定」と出る)。両方に出すと
  同じ手術が 2 か所に見え、右へ落としても戻ってくる行き止まりの操作になる。
- ［導出］逆に**部屋未定は週ビューでも出す**。セルは部屋で絞るので、部屋の無い手術は
  週の格子のどこにも描かれない —— 右ペインが唯一の置き場になる。
- 日付未定は見ている日/週に関係なく全件出す(手術部が組む相手そのもの)。
- 空の組は見出しごと出さない。3 つの見出しが常に並ぶと何が残っているのか読みにくい。
- 中止は格子と同じく数えない(`roomDayRows`)。
- ドラッグを使わない経路として、カードのケバブに「日程を確定」(一覧タブと同じ
  `SurgeryScheduleModal`)と「カルテを表示」を置く。

#### ドラッグの落とし先

格子のカードと**同じ `useCardDrag` / 同じ確認モーダル**を通る。右から落としたときも
`SurgeryMoveTarget` の作り方は変わらない。

- 日ビュー: 落とした列と高さが そのまま 手術室・入室時刻になり、日付は見ている日。
- 週ビュー: 落としたセルの日付と手術室だけ。時刻は動かさない(§2.3 と同じ)ので、
  日付未定を週へ落とすと「日付・部屋は決まったが時刻未定」になる —— 続きは日ビューで。
- ［実装］**右ペインから掴んだときは掴みしろを 0 にする**。掴みしろ(§2.3)は
  カードが時間軸に載っていることが前提で、部屋未定の手術は入室時刻を持つため、
  そのままだと掴んだ位置と落ちる位置がずれる。
- ［提案］**Task には触らない**(§2.3 と同じ `buildSurgeryMoveBundle`)。日付未定を
  格子へ落とすと申込済のまま予定日別に並ぶが、これは希望日を書いて申し込まれた
  オーダーと同じ状態で、そこから「受付」を押す流れも一覧タブと同じ。受付済へ
  進めたいときはケバブの「日程を確定」を使う。
- 変更前が空ばかりになるので、確認モーダルの見出しだけ
  「手術の日程を移動」→「手術の日程を決める」に変える。

#### 分割

［事実］スプリッタはカルテのペイン境界(`KarteSplitter`)をそのまま使う。向きは
`vertical`、比率は格子側が占める割合で、**日ビューと週ビューで共有**する
(モードを切り替えるたびに幅が戻ると、右を広げて日程を組んでいる最中に邪魔になる)。
位置は `surgeryCalendarLayout.ts` が localStorage に残す(0.4〜0.9 に丸める)。
狭い画面(900px 以下)では左右に割らず縦に積み、スプリッタは隠す。

### 2.5 読み切れなかったとき

［導出］`truncated` のときは「重なりなし」も「件数」も嘘になる。日ビュー・週ビュー
どちらでもその旨を必ず出す(`SurgeryRoomDaySchedule` と同じ扱い)。

### 2.6 カレンダーからの登録・修正

［提案］**カルテ右ペインと同じフォームをモーダルで開く**(`SurgeryOrderModals` が
`SurgeryOrderCreatePanel` / `SurgeryOrderEditPanel` をそのまま包む)。手術部が使う
入口を増やすだけで、申込の中身(術式・スタッフ・麻酔・準備)は二重に持たない。
できるオーダーもカルテからの申込と同じ —— `buildSurgeryOrderBundle` は Task を
作らないので、進捗は「申込済」から始まる。

#### 新規登録: 空き枠を掴む

日ビューの列の空いているところを**縦に引く**と、その範囲(日付・入室時刻・所要時間・
手術室)を初期値にした登録フォームが開く。押しただけなら所要時間は空にして、術式を
選んだときのマスタ既定値に任せる(`SurgeryOrderForm.addItem` は
`current.durationMinutes ||` なので、引いて決めた値は上書きされない)。
カレンダーは患者が決まっていないので、入院登録(`AdmissionModal`)と同じ
**患者を選ぶ → 中身を書く**の 2 段階にし、検索は `AdmissionPatientSearch` を借りる。

［改訂］その患者検索に**入外区分と入院病棟**の絞り込み、および入院病棟の列を足した
(入院登録・入院予定でも同じものが出る)。手術の申込は「あの病棟のあの人」で探すことが
多く、患者番号も氏名もうろ覚えのまま開く場面があるため。列の並び(患者番号 → 氏名(カナ) → 生年月日(年齢)
→ 性別)は部門業務の各ワークリストにそろえ、カナ・生年月日・性別は共通のセル
(`PatientRowCells`)を使う。

［実装］入院(または病棟)を選ぶと**引き元が変わる**。患者検索(`usePatientSearch`)は上流で
ページングするので、返ってきた 1 ページを間引くと件数も次ページも嘘になる。入院で絞る
ときは在院患者(`useInpatientEncounters`。患者も `_include` 済み)から作り、患者番号・氏名は
その中で絞る —— 在院ぶんは手元で絞りきれる数に収まる。ページ送りは出さず件数だけ出す。
病棟を選んだ時点で入院を選んだのと同じ扱いにする(病棟のある外来は無い)。

［実装］**外来だけは間引きになる**。「入院していない」は上流に問い合わせようが無く
(FHIR に否定の検索は無い)、在院患者の集合から作ることもできない。検索結果から入院中を
落とす形にし、ページ送りの件数と行数がずれるぶんは「入院中の N 件をこのページから
除いています」と**その場で断る**。外来を選んでいる間は病棟のセレクトを無効にする。

［実装］入院(Encounter)が持つのは**ベッドだけ**で、`display` も「301号室 ベッド1」なので
病棟名を含まない。病棟は ベッド → 病室 → 病棟 の partOf を辿る必要があるので、
`useBedWardIndex` が **全病棟をまとめて 1 本**で引いた索引(ベッド id → 病棟)を配る
(`partof` のカンマ OR + `_revinclude=Location:partof`。子は `_count` の対象外)。

［導出］**引いている間ずっと入室〜退室と所要時間を出す**(`.surgery-calendar__slot-select`)。
枠を決める操作で見たいのは終わりの位置ではなく「何分か」で、それはフォームに入る値
そのもの。見出しにも同じ範囲を出して、モーダルが開いた後もどの枠か分かるようにする。

［導出］**掴む前にマウスの下の 1 枠を薄く塗る**(`.surgery-calendar__slot-hover`)。
列の中は罫線が 1 時間おきにしか無く、どこから始まるのかが掴む前に分からないため。
落とし先(点線)より弱い塗りにして、カードを掴んでいる間は出さない。

［導出］刻みはカードの移動(5 分)より粗い **15 分**。始まりは切り捨て、終わりは
切り上げて、掴んだ範囲を必ず含む 1 枠以上にする。上へ引いても下へ引いても同じ。

［実装］カード(とカード上のボタン・ケバブ)の上は除く。`RowMenu` は非ポータルなので
`closest(".surgery-calendar__card")` で見分けられる。割当科の帯は背景でしかないので
その上も空き枠として扱う。

［実装］**click ではなく pointerup で開く**。カードを掴んで同じ列に落とすと click は
列の本体まで上がってくるので、click で開くと移動確認と登録が同時に出る。pointerdown
がカードの上かどうかで入口を分ければ、両者は最初から混ざらない。

［実装］掴んでいる範囲は state だけでなく **ref にも持つ**。押してすぐ離すと
pointerdown の `setState` が反映される前に pointerup が走り、state だけだと
「押しただけ」を取り落とす。列の外へ出ても引き続けられるように
`setPointerCapture` する。

［提案］週ビューには空き枠登録を置かない。セルは 1 日ぶんの箱で入室時刻も所要時間も
決められず、押したら日ビューへ降りる既存の動きをそのまま残す。

#### 修正: カードのケバブから

日ビューのカード・週ビューのチップ・未確定リストのカードの 3 か所のケバブに「編集」を
足す。開くのはカルテと同じ編集フォームなので、**編集できる範囲も可否もカルテと同じ**
(進捗では絞らない) —— 入口で差をつけると「カルテでは直せるのにカレンダーでは直せない」
になる。患者は `_include` で来た `row.patient` を第一候補に、無ければ
`order.subject` から拾う。

［実装］登録の mutation(`useCreatePrescription`)は他オーダーと共用で
`["ServiceRequest","search"]` しか無効化しない。カレンダーは別のキーなので、
登録直後のカードを出すために `SurgeryOrderCreateModal` の側で
`surgery-worklist` / `surgery-unscheduled` を落とす。更新
(`useUpdateSurgeryOrder`)は `["ServiceRequest"]` を丸ごと無効化するので何も要らない。

［実装］依頼科・依頼医師はヘッダーの `OrderContextPicker` を使う。カルテ画面
限定の表示だったものを**常時表示**にした(オーダーを作る画面がカルテだけでは
なくなったため)。未選択でも登録は通る(`applyOrderContext` が requester を付けない
だけ)ので、モーダルの先頭に注記を出すに留める。

---

## 3. ダブルブッキングの防止

［提案］**重なりを検知したら登録を一旦止め、「重なりを承知で登録」を明示した
ときだけ通す**。

第1段階の §5.5 では「警告まで。登録は止めない」と導出していた。緊急の割り込みや、
前の手術が早く終わる読みでわざと詰めることがあり、止めると運用が回らないため。
一方 §7-3 は「競合するオーダーをそもそも作れなくすること」を残タスクにしていた。
確認 1 回ぶんの手数で両立させる —— 意図して詰めるときは 1 クリック増えるだけ、
うっかりの二重予約はここで気づける。

### 3.1 差し込む場所

日程が書かれる経路は 2 つで、どちらにも同じ確認を入れた。

| 経路 | 画面 |
|---|---|
| 手術部の日程確定 | `SurgeryScheduleModal` |
| 診療科の申込・カルテからの日程変更 | `SurgeryOrderForm` |

手順は `useSurgeryConflictCheck` に切り出してある。

1. バリデーションを通す
2. **キャッシュを見ずに**その日の一覧を引き直す(`fetchQuery` + `staleTime: 0`)
3. 判定は `fhir/surgeryConflictHelpers` の純関数(表示・カレンダーと同じもの)
4. 重なりゼロならそのまま登録。あれば `SurgeryConflictConfirmModal` を出す

［導出］キャッシュを見てはいけない。一覧を開いた直後に確定モーダルを開くと
キャッシュがそのまま効き、「一覧を見た後に他端末が入れたぶんを拾う」というこの
確認の目的そのものが果たせない。

### 3.2 確認しないで通す場合

日付・手術室・入室時刻のどれかが未定なら取り合う相手が定まらないので、確認せず通す。
部屋未定のまま日程だけ入れる申込は普通にあり、そこで確認を出しても答えようがない。

### 3.3 確かめられなかった場合

［導出］一覧の読み込みに失敗した/読み切れなかった(`truncated`)ときは、重なりが
無いと言い切れない。**重なりゼロでも確認モーダルを出す**(「確かめられませんでした」
と伝える)。黙って通すと、確認を挟んだこと自体が嘘になる。

### 3.4 サーバー側の排他は入れない

［提案］上流 FHIR サーバーに手術専用の予約テーブルを足す話になり、Slot を使わない
判断(§1)と釣り合わない。ここで塞げるのは「一覧を見てから登録するまでの間に他の
端末が入れた」ぶんで、残るのは submit の前後数秒に同時登録が重なるケースだけ。
麻酔チャートの同時編集(`anesthesia-chart-design.md` §7-5)と同じく、運用
(手術部が日程を握る)で足りているうちは楽観のままにする。

### 3.5 window.confirm は使わない

重なっている相手を一覧で見せられないのと、ブラウザ自動化で操作が詰まるため。
`Modal` は非ポータルで、申込フォームからは外側の `<form>` の内側に描かれる。
**確認モーダルに `<form>` を置いたりボタンの `type` を省いたりすると外側の
フォームがネイティブ送信される**ので、`type="button"` を必ず付ける。

---

## 4. ブロックスケジュール(曜日ごとの科割り当て)

「月曜の第1手術室 9:00-12:00 は外科」という運用上の取り決め。

### 4.1 置き場所

［提案］**backend のマスタに置く**(`master_surgery_room_blocks`)。FHIR の
`Schedule` を使わないのは、予約が紐付かない純粋な施設設定で、読むのはカレンダーの
背景と申込フォームの警告だけだから。§1 で Slot を捨てた以上、Schedule だけ持っても
予約機構には繋がらない。

| 列 | 内容 |
|---|---|
| `location_id` / `location_name` | 手術室(Location 種別 `SU`)の id と名称の写し |
| `weekday` | 0=日 … 6=土(`SlotPattern`・`Date#getDay` と同じ並び) |
| `start_time` / `end_time` | "09:00" 形式 |
| `department_code` / `department_name` | SS-MIX2 統一診療科コードと名称の写し |
| `valid_from` / `valid_to` / `note` | 他マスタと同型 |

- 名称を写しで持つのは `Schedule.actor.display` と同じ考え方(Location /
  Organization が消えても行の意味が読める)。表示は今の Location 名を優先する。
- 検索用の `search_*` 列は持たない。名称で引くマスタではなく、部屋と曜日で
  全件を読んで使うため。
- **同じ部屋・同じ曜日で時間帯が重なる割り当てはモデルで弾く**。1 つの時間帯を
  2 つの科に割り当てると、警告のどちらを出せばよいか決められない。ただし
  有効期間が重ならないもの(年度で入れ替える運用)は重複と見なさない。

### 4.2 使い方は警告だけ

［提案］**割当外の科でも登録は止めない**。割当科が使わない枠を前日に他科へ回す運用が、
マスタを触らずに回るようにするため。ここを固くすると、融通のたびに割当変更か
ブロック解除の操作が要る。

- カレンダー: 日ビューは列の背景帯、週ビューはセル頭のチップ。
- 申込・日程確定: `SurgeryRoomDaySchedule` に「割当」行を出し、入れようとしている
  時間帯が別の科の割当に掛かっていれば「この時間帯は◯◯科の割当です」を出す。
- **確認モーダルの対象は時間の重なりだけ**。割当外は警告に留める(止める基準を
  2 つ持つと、どちらで止まったのか読み取れない)。

### 4.3 全件を読む

［事実］`Master::BaseController#pagination_params` は 1 ページ 100 件が上限。
手術室 × 曜日 × 時間帯は部屋が増えるとすぐ 100 を超えるので、
`fetchAllSurgeryRoomBlocks` がページを最後まで読む。「その部屋のその曜日」を
引き当てられないと警告が嘘になるため、ここだけは読み切る。

---

## 5. 実装したもの

| 層 | 追加物 |
|---|---|
| migration | `20260828100000` ブロックスケジュール |
| モデル | `Master::SurgeryRoomBlock` |
| API | `surgery_room_blocks`(index/show/create/update/destroy) |
| spec | リクエスト 1 本(14 examples) |
| FHIR 変換 | `fhir/surgeryConflictHelpers.ts`(重なり・割当の判定。`SurgeryRoomDaySchedule` から移した) |
| フック | `hooks/useSurgeryConflictCheck.ts`、`api/queries.ts` の `surgeryWorklistQuery` / `useSurgeryWorklistWeek` |
| 画面 | `SurgeryCalendar`(日/週)、`SurgeryConflictConfirmModal`、`SurgeryRoomBlockPage` |
| 画面 | `SurgeryCalendarPage`(`/surgery-calendar`。日付と表示単位を持つだけの入れ物) |
| 既存の改修 | `SurgeryWorklistPage`(カレンダータブを外した)、`SurgeryOrderForm` / `SurgeryScheduleModal`(登録前の確認)、`SurgeryRoomDaySchedule`(割当の表示 + 判定の共通化) |
| ドラッグ | `hooks/useCardDrag.ts`、`SurgeryMoveConfirmModal`(移動・リサイズ共通。`SurgeryMoveTarget.durationMinutes` を足した)、`buildSurgeryMoveBundle` / `useMoveSurgerySchedule`、`surgeryConflictHelpers` の `isSurgeryMovable` / `snapMinutes` |
| リサイズ | `RoomColumn` の `startResize` / `resizedTo`、`SurgeryCard` の縁(`.surgery-calendar__card-resize`)|
| 未確定リスト | `SurgeryPendingPanel`(§2.4)、`surgeryCalendarLayout.ts`(分割位置の保存)、`KarteSplitter` の再利用 |
| 登録・修正 | `SurgeryOrderModals`(`SurgeryOrderCreateModal` / `SurgeryOrderEditModal`。中身はカルテと同じ `SurgeryOrderCreatePanel` / `SurgeryOrderEditPanel`、患者検索は `AdmissionPatientSearch` を再利用)、`SurgeryOrderCreatePanel` の `defaultSchedule`、`RoomColumn` の空き枠の下見・範囲選択(`SLOT_SNAP_MINUTES`)、`App` のヘッダー(`OrderContextPicker` を常時表示) |
| カード | `SurgeryCalendar` の `SurgeryCard` / `SurgeryCardActions`(患者情報は `PatientKana` / `ageWithMonthsLabel` / `genderLabel`、操作は `surgeryTaskActions` / `useUpdateSurgeryTaskStatus` / `SurgeryPerformModal` を一覧タブと共用) |

### 5.1 §5.5 の表示は残す

`SurgeryRoomDaySchedule`(日程を触る画面に、その日その部屋の予定を出す)は
カレンダーが入っても捨てない。カレンダーは別画面で、日程を入れている最中には
見えないため。第1段階の §5.5 の判断のとおり。

---

## 6. 申し送り

1. **多資源(執刀医・麻酔科医)の空き**: 今のカレンダーは手術室だけ。執刀医の
   重複(同じ医師が同じ時間に 2 部屋)は判定していない。`summarizeSurgeryOrder` の
   `staff` に執刀医が入っているので、判定は `surgeryConflictHelpers` に足せる。
   ［提案］運用で困ってから入れる —— 執刀医の重複は診療科の中で見えているのに対し、
   部屋の重複は誰にも見えないという非対称があって、後者から塞いだ。
2. **月ビュー**: 週より広い範囲の空き探し。7 本のクエリを 30 本に増やすことになるので、
   日付ごとの件数だけを返す上流の `$distinct-dates`(件数モード。予約枠の月カレンダーが
   使っている)に相当するものが `ServiceRequest` にも要る。
3. **週ビューでの時間の変更**: 日ビューはカードの縁で伸縮できる(§2.3)が、週ビューの
   チップは 1 日ぶんの箱の中の行で、縦位置が時刻を表していないので掴めない。週で
   時間を詰めたくなったら日ビューへ降りる。
4. **ブロックスケジュールの一括生成**: 今は 1 行ずつ登録する。`SlotGenerateModal` と
   同じ「曜日 × 時間帯をまとめて作る」が欲しくなったら足す。
5. **祝日**: 週ビューの曜日色は土日だけ。祝日マスタを持っていないので、祝日に
   割当が出たままになる。
6. **未確定リストのキーボード操作**: 格子への移動はドラッグだけで、キーボードからは
   ケバブの「日程を確定」(モーダルで入力)を通る。格子のセルにフォーカスを持たせて
   移動先を選ぶ操作は持っていない。
